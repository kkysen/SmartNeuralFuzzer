set(target pass.coverage.branch.execute2)
addCoverageRuntimePathDefinition(branch.execute)
add_compile_definitions(PASS_NAME=\"${target}\")
add_library(${target} SHARED BranchExecutionPass.cpp ${files.share})
copyLibrary(${target})
